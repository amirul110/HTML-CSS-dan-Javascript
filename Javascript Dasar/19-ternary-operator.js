const nilai = 23;

// ternary operator itu if else dalam 1 baris
            // kondisi        nilai true            // nilai false
const reward = nilai >= 23 ? "selamat anda lulus" : "silahkan coba lagi NANTIIIIIIIIIIIIII"

console.log(` ${reward}`) //selamat anda lulus
